[project]
name = "workout-tracker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.0",
    "flask-sqlalchemy>=3.1",
]

[project.optional-dependencies]
# Only the container needs these: the dev server in main.py does not.
deploy = [
    "gunicorn>=23.0",
]


[dependency-groups]
dev = [
    "pytest>=8.0",
]